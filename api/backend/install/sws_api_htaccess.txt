# ============================================================
# sws/api/ – Verzeichnisschutz
# ============================================================

# Verzeichnislisting deaktivieren
Options -Indexes

# Service-Account-JSON vor Direktzugriff schützen
<FilesMatch "swsfb-11c77-firebase-adminsdk-fbsvc-7dc4d2384c\.json$">
	Require all denied
</FilesMatch>

# Alle anderen JSON-Dateien ebenfalls sperren (Fallback)
<FilesMatch "\.json$">
	Require all denied
</FilesMatch>
